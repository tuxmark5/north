pub mod reduce_lval;
pub mod reduce_mod;
pub mod reduce_type;
pub mod reduce_value;

pub use self::{
  reduce_type::type_of_reduced,
};

////////////////////////////////////////////////////////////////////////////////////////////////

use {
  lang_mir::mir::BlockId,
  north_core::prelude::*,
};

////////////////////////////////////////////////////////////////////////////////////////////////

impl_prop!(P_LOOP_DESCR, LoopDescr);

#[derive(Clone)]
pub struct LoopDescr {
  loop_start: BlockId,
  loop_end: BlockId
}

////////////////////////////////////////////////////////////////////////////////////////////////

impl_prop!(P_VAR_MUTABLE, bool);

////////////////////////////////////////////////////////////////////////////////////////////////
